export interface Vehicle {
  id: number;
  name: string;
  registrationNumber: string;
  capacity: number;
}

export interface VehicleCommand {
  name: string;
  registrationNumber: string;
  capacity: number;
}